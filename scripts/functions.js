var tubetotv = {};(function() {	this.browsertooltip = function(timeout, text){		notify = webkitNotifications.createNotification("images/browseraction.png", "", text);		notify.show();		setTimeout(function () {			notify.cancel();		}, timeout);		};  	this.connect = function(usr, pw, ip, callback){		xmlrequest=new XMLHttpRequest();		//need username + pw?		if (usr){		   xmlrequest.open('POST','http://' + usr + ':' + pw + '@' + ip + '/jsonrpc', true);		}else{		   xmlrequest.open('POST','http://' + ip + '/jsonrpc', true);		}		   xmlrequest.setRequestHeader('Content-type','application/json');		callback(xmlrequest);		};}).apply(tubetotv);